import Foundation
import Must-bKit

// Prefer the Must-bKit wrapper to keep gateway request payloads consistent.
typealias AnyCodable = Must-bKit.AnyCodable
typealias InstanceIdentity = Must-bKit.InstanceIdentity
