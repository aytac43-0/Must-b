import Foundation
import MustBKit

// Prefer the MustBKit wrapper to keep gateway request payloads consistent.
typealias AnyCodable = MustBKit.AnyCodable
typealias InstanceIdentity = MustBKit.InstanceIdentity
