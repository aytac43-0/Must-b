---
summary: "CLI reference for `must-b clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `must-b clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `must-b clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `must-b clawbot qr` (same behavior as [`must-b qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `must-b clawbot qr` -> `must-b qr`
