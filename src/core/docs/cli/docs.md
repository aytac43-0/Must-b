---
summary: "CLI reference for `must-b docs` (search the live docs index)"
read_when:
  - You want to search the live Must-b docs from the terminal
title: "docs"
---

# `must-b docs`

Search the live docs index.

```bash
must-b docs browser extension
must-b docs sandbox allowHostControl
```
